export enum MeteorComponentType {
    Button = 'button',
    Select = 'select',
    Modal = 'modal'
}

export enum MeteorSelectType {
    String = 'string',
    Channel = 'channel',
    Role = 'role',
    User = 'user',
    Mentionable = 'mentionable'
}