import { EmbedBuilder } from 'discord.js';
import { ButtonType } from '../../types/button';

export default {
    id: 'deny_background',
    async execute(interaction) {

    }
} as ButtonType;